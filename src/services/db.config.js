module.exports = {
    HOST: "127.0.0.1",    // (1) ที่อยู่ฐานข้อมูล (ถ้าทำในเครื่องตัวเองใช้ 127.0.0.1 หรือ localhost)
    USER: "root",         // (2) ชื่อผู้ใช้ (ปกติ XAMPP ใช้ 'root')
    PASSWORD: "",         // (3) รหัสผ่าน (ปกติ XAMPP จะว่างไว้ ถ้าใช้ MAMP อาจเป็น 'root')
    DB: "kids_db"         // (4) ชื่อฐานข้อมูล (เอามาจากไฟล์ SQL ของคุณ)
};